import 'package:flutter/material.dart';

class CreatePage extends StatelessElement{
  @override
  Widget build() {
    return Scaffold(
      appBar: AppBar(title: Text("등록"),),
      body: Text("등록페이지"),
    );
  }
}